# Libraries

Place ArgiSense application libraries and helper modules here.

The top-level `Kconfig` and `CMakeLists.txt` already include this directory, so future libraries can be added as subdirectories without changing the repository architecture.

Expected future library locations include:

```text
lib/mqtt_backend
lib/updatehub_helpers
```
