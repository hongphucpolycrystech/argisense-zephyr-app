# Drivers

Place out-of-tree Zephyr drivers here.

The top-level `Kconfig` and `CMakeLists.txt` already include this directory, so future drivers can be added as subdirectories without changing the repository architecture.

Expected future driver locations include:

```text
drivers/sim7600
drivers/mlx90640
```
